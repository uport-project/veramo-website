version: 2
aliases:
  - &check-code-format
    name: Check code formatting
    command: yarn prettier --check

  - &restore-cache
    keys:
      - v2-dependencies-{{ .Branch }}-{{ checksum "package.json" }}
      - v2-dependencies-{{ .Branch }}
  - &save-node-cache
    key: v2-dependencies-{{ .Branch }}-{{ checksum "package.json" }}
    paths:
      - node_modules

  - &install-node-dependencies
    name: Install node dependencies
    command: yarn install --frozen-lockfile

defaults: &defaults
  working_directory: ~/website

jobs:
  check-formatting:
    <<: *defaults
    docker:
      - image: node:10
    resource_class: small
    steps:
      - checkout
      - restore_cache: *restore-cache
      - run: *install-node-dependencies
      - run: *check-code-format
      - save_cache: *save-node-cache

  build-api-docs:
    working_directory: ~/daf
    docker:
      - image: node:10
    resource_class: small
    steps:
      - run:
          name: Checkout Daf
          conmand: git clone git@github.com:uport-project/daf.git .
      - run:
          name: Build Daf Packages
          command: yarn && yarn build

workflows:
  version: 2

  verify-build-local-docs:
    jobs:
      - check-formatting
      - build-api-docs:
          requires:
            - check-formatting
